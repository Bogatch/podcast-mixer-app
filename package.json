{
  "name": "podcast-mixer-studio",
  "version": "1.2.0",
  "description": "Nástroj na nahrávanie a vizuálne mixovanie zvukových stôp pre podcasty.",
  "scripts": {
    "build:ui": "rm -rf public && mkdir -p public && cp index.html public/ && esbuild index.tsx --bundle --outfile=public/bundle.js --minify --sourcemap --platform=browser --format=iife"
  },
  "author": "CustomRadio.sk",
  "license": "UNLICENSED",
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4",
    "jszip": "^3.10.1",
    "lamejs": "^1.2.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}