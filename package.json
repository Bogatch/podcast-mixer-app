{
  "name": "podcast-mixer-studio",
  "version": "1.3.0",
  "description": "Nástroj na nahrávanie a vizuálne mixovanie zvukových stôp pre podcasty.",
  "scripts": {
    "build:js": "esbuild index.tsx --bundle --outfile=dist/bundle.js --minify --format=esm --external:react --external:react-dom/client --external:jszip --external:@google/genai",
    "build:html": "cp index.html dist/index.html",
    "build": "rm -rf dist && mkdir dist && npm run build:html && npm run build:js",
    "build:ui": "npm run build"
  },
  "author": "CustomRadio.sk",
  "license": "UNLICENSED",
  "dependencies": {
    "@google/genai": "^0.12.0",
    "abort-controller": "^3.0.0",
    "jszip": "^3.10.1",
    "node-fetch": "^2.7.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "stripe": "^15.12.0"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.11",
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "@vercel/node": "^3.0.27",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}